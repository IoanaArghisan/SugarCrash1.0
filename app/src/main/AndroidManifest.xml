<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sugarcrash10">

    <supports-screens>
        android:resizeable="true"
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true"
    </supports-screens>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SugarCrash10">
        <activity android:name=".MainActivityValoriGlicemie"></activity>
        <activity android:name=".MainActivityGlicemie" />
        <activity android:name=".MainActivitySport" />
        <activity android:name=".MainActivityRetete" />
        <activity android:name=".MainActivityApa" />
        <activity android:name=".MainActivityInformatii" />
        <activity android:name=".MainActivityMeniu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>